<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendanceapp2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".AttendanceApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AttendanceActivity"></activity>
        <activity android:name=".QRCodeGenerator" />
        <activity android:name=".ScanningActivity" />
        <activity android:name=".StudentInfo" />
        <activity android:name=".StudentList" />
        <activity android:name=".ClassInside" />
        <activity android:name=".InviteeActivity" />
        <activity android:name=".HostActivity" />
        <activity android:name=".RegisterInvitee" />
        <activity android:name=".RegisterHost" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>